string reverseWord(string str){
    int start,end;
    start=0;
    end=str.length()-1;
    for(int i=0;i<end;i++){
        swap(str[start],str[end]);
        start++;
        end--;
    }
    return str;
}
