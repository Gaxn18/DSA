class Solution
{
    public int find_median(int[] v)
    {
        Arrays.sort(v);
        int median;
        int n=v.length;
        if(n%2==0)
            median=(v[n/2-1]+v[(n/2)])/2;
        else
            median=v[n/2];
        return median;
        // Code here
    }
}
